---
apiVersion: v1
kind: Pod
metadata:
  name: nodeselector-pod
spec:
  containers:
    - name: mike-cont1
      image: nginx
      ports:
        - containerPort: 80
          protocol: TCP
  nodeSelector:
    beta.kubernetes.io/os: linux
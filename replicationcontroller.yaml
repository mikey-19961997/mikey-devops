---
apiVersion: v1
kind: ReplicationController
metadata:
  name: my-rc
spec:
  minReadySeconds: 3
  replicas: 5
  selector:
    app: nginx
    ver: "1.23"
  template:
    metadata:
      name: mikey-pod4
      labels:
        app: nginx
        ver: "1.23"
    spec:
      containers:
        - image: nginx:1.23
          name: mike1
          ports:
            - containerPort: 80
              protocol: TCP